---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: gitlab
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - gitlab.dev.erpf.de
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: "SIMPLE"
        credentialName: gitlab-dev-tls
      hosts:
        - gitlab.dev.erpf.de

---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: registry
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - registry.dev.erpf.de
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: "SIMPLE"
        credentialName: registry-dev-tls
      hosts:
        - registry.dev.erpf.de

---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: minio
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - minio.dev.erpf.de
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: "SIMPLE"
        credentialName: minio-dev-tls
      hosts:
        - minio.dev.erpf.de