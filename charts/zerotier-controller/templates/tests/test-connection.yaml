apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zerotier-controller.fullname" . }}-test-connection"
  labels:
    {{- include "zerotier-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zerotier-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
